const config = {
    db: {
        host: '{HOST}',
        user: '{USER}',
        password: '{PASSOWRD}',
        database: '{DB}',
    },
    encryption: {
        secret: '{SECRET}',
        iv: '{IV}',
    },
};

module.exports = config;
